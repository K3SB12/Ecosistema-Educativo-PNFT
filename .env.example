# Variables de entorno (solo para backend opcional, no necesario para frontend)
NODE_ENV=development
PORT=5000
MONGODB_URI=mongodb://localhost:27017/pnft
JWT_SECRET=tu_secreto_muy_largo
# Opcional: API keys para IA (cada docente usa la suya, no se incluyen en el código)
GOOGLE_AI_API_KEY=
OPENAI_API_KEY=
